import type { Metadata } from "next"; import { ContactForm } from "@/components/contact-form"; import { PageIntro } from "@/components/site-shell"; import { contact } from "@/data/site";
export const metadata: Metadata = { title: "Contact", description: "Parlez-nous de votre territoire digital." };
export default function Contact() { return <><PageIntro eyebrow="Le premier virage" title="Parlons de votre projet.">Une idée, une activité, un système à simplifier ? Donnez-nous le contexte. Nous vous répondons sans jargon inutile.</PageIntro><section className="section grid gap-7 pt-8 lg:grid-cols-[1fr_.35fr]"><ContactForm /><aside className="glass h-fit p-6"><p className="eyebrow">Contact direct</p><a className="mt-5 block hover:text-lime-200" href={`mailto:${contact.email}`}>{contact.email}</a><a className="mt-2 block hover:text-lime-200" href={`tel:${contact.phone.replaceAll(" ", "")}`}>{contact.phone}</a><p className="mt-6 text-sm leading-6 text-white/55">Le formulaire est prêt à être branché à votre automatisation via webhook.</p></aside></section></>; }
