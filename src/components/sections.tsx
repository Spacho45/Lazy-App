import Image from "next/image"; import Link from "next/link"; import { environments, faqs, steps, tools } from "@/data/site";
export function CTA({ compact = false }: { compact?: boolean }) { return <section className={compact ? "py-8" : "section"}><div className="glass fire-border flex flex-col items-start gap-5 p-7 md:flex-row md:items-center md:justify-between"><div><p className="eyebrow">Une idée à mettre sur la route ?</p><h2 className="mt-2 text-2xl font-bold md:text-4xl">Faisons simple. Mais faisons-le vraiment.</h2></div><Link href="/contact" className="btn-primary shrink-0">Mon identité sur-mesure</Link></div></section>; }
export function StepsGrid({ limit }: { limit?: number }) { return <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">{steps.slice(0, limit).map((step) => <article key={step.number} className="glass card-hover p-6"><p className="mono text-acid">{step.number} / {step.metaphor}</p><h3 className="mt-5 text-2xl font-bold">{step.title}</h3><p className="mt-3 text-sm leading-6 text-white/60">{step.summary}</p><div className="mt-5 flex flex-wrap gap-2">{step.items.map((item) => <span className="tag" key={item}>{item}</span>)}</div></article>)}</div>; }
export function EnvironmentsGrid() { return <div className="grid gap-5 lg:grid-cols-3">{environments.map((env) => <article key={env.name} className={`glass card-hover overflow-hidden accent-${env.accent}`}>{env.image ? <div className="relative h-52 overflow-hidden"><Image src={env.image} alt={`Univers ${env.name}`} fill className="object-cover object-top opacity-85" /></div> : <div className="just-socialing flex h-52 items-end p-6"><span className="mono text-xs text-black">SIGNAL / ATTENTION / IMPACT</span></div>}<div className="p-6"><p className="eyebrow">{env.eyebrow}</p><h3 className="mt-2 text-2xl font-bold">{env.name}</h3><p className="mt-3 text-sm leading-6 text-white/60">{env.description}</p><p className="mt-5 mono text-xs text-white/45">{env.tone}</p></div></article>)}</div>; }
export function ToolsGrid() { return <div className="grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-5">{tools.map((tool, index) => <div className="glass card-hover p-5" key={tool}><span className="mono text-xs text-acid">0{index + 1}</span><h3 className="mt-6 font-bold">{tool}</h3></div>)}</div>; }
export function FAQ() { return <section className="section"><p className="eyebrow">FAQ</p><h2 className="section-title">Les questions utiles, sans détour.</h2><div className="mt-8 grid gap-3">{faqs.map((faq) => <details className="glass group p-5" key={faq.question}><summary className="cursor-pointer list-none font-semibold">{faq.question}<span className="float-right text-acid group-open:rotate-45">+</span></summary><p className="mt-4 max-w-4xl text-sm leading-6 text-white/60">{faq.answer}</p></details>)}</div></section>; }
